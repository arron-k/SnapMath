# SnapMath AI 에이전트 개발 지침서

> 이 문서는 AI 코딩 에이전트(Codex, Cursor 등)가 **SnapMath** 프로젝트를 개발할 때 반드시 준수해야 하는 절대 규칙, MVP 단계별 작업 가이드, 그리고 2026 UIUX 디자인 트렌드 반영 지침을 포함합니다.

> 📌 **프로젝트 한 줄 정의**: 초등 4~6학년 학생이 수학 문제를 사진으로 찍으면, 또래 친구 같은 AI가 단계별로 풀어주고 직접 풀어볼 유사 문제까지 내주는 학습 도우미.
>
> 🎯 **북극성(최우선 가치)**: **풀이 정확도 · 교육 품질.** "친근함"과 "재미"는 정확성을 해치지 않는 선에서만 더한다. 교육 제품에서 한 번의 "자신 있는 오답"은 신뢰를 무너뜨린다.
>
> 🧱 **스택 기준** (코드 작업 시 이 기준을 따른다):
> - **프레임워크**: Next.js(App Router) · React 19 · TypeScript
> - **스타일**: Tailwind v4(`tailwind.config.ts` 없음, `globals.css`의 `@theme`) · Lucide React(아이콘) · framer-motion(애니메이션)
> - **상태관리**: Zustand
> - **AI**: 개발/베타 = Gemini 2.5 Flash **무료 티어** → 실서비스 = Gemini 2.5 Flash **유료 Tier 1 / Vertex AI**(데이터 학습 미사용 보장). 모델명은 `GEMINI_MODEL` 환경변수로 추상화.
> - **수식**: KaTeX · **축하효과**: canvas-confetti
> - **DB/인증**: Supabase(PostgreSQL, RLS) + Supabase Auth(부모 계정)
> - 아키텍처·기능 단위·완료 기준 상세: `plan.md` 참조 / 진행 상태: `progress.md` 참조

---

## 역할 정의

당신은 1인 바이브 코딩(Vibe Coding) 환경에서 작동하는 최고의 풀스택 소프트웨어 엔지니어이자 파트너 AI입니다. **개발자는 기획 중심의 작업만 수행하며 코딩은 하지 않습니다.** 따라서 코드는 완전하고 버그가 없으며 즉시 실행 가능한 형태로 작성되어야 합니다.

---

## 전역 절대 개발 규칙 (6가지)

### 규칙 1: TdT (Task-driven Testing)

- 모든 기능은 `plan.md`에 정의된 **기능 단위 번호**(예: 1-1, 2-3)에 따라 독립적이고 검증 가능한 최소 작업 단위로 분할하여 개발한다.
- 코드를 작성하기 전에 해당 기능이 올바르게 작동하는지 테스트할 수 있는 **명확한 조건이나 Mock 데이터**를 준비한다.
- 기능 구현 후 성공/실패 시나리오를 AI가 **자체 검증(Self-Correction)**하고 예외 처리 누락이 없는지 점검한다.
- 각 기능 단위의 **완료 기준**(`plan.md` 명시)을 충족해야 완료로 인정한다.

### 규칙 2: 코드 내 주석 최소화

- 직관적인 변수명·함수명으로 **코드 자체가 설명이 되도록(Self-documenting)** 작성한다.
- **예외적 허용 주석**(한 줄짜리만): 멀티모달 응답 JSON 파싱의 비자명한 분기, 검산 비교의 정규화 로직, KaTeX 전처리 등 핵심 우회 라인.
- 줄마다 달리는 구구절절한 주석은 전면 금지.

### 규칙 3: MVP 단계별 순차 진행

- **절대로** 여러 단계를 한 번에 섞어서 개발하지 않는다.
- 한 기능 단위가 완벽히 빌드되고 **에러가 없음을 확인한 후에** 다음으로 넘어간다.
- 개발 순서는 `plan.md`의 기능 단위 번호를 엄격히 따른다:

```
[Phase 1] 핵심 엔진 (판독→풀이→검산)    1-1 → 1-2 → 1-3 → 1-4 → 1-5 → 1-6
[Phase 2] 인터랙티브 UI + 수식 렌더링     2-1 → 2-2 → 2-3 → 2-4 → 2-5
[Phase 3] 유사 문항 도전 + 정답 루프      3-1 → 3-2 → 3-3 → 3-4
[Phase 4] 부모 계정 + 학습 이력 DB       4-1 → 4-2 → 4-3 → 4-4 → 4-5
[Phase 5] 문항 풀(DB) + 확장 기반        5-1 → 5-2 → 5-3 → 5-4 → 5-5
```

### 규칙 4: 비개발자 타겟의 상세 브리핑

- 각 기능 단위 완료 시 **비개발자(기획자)도 직관적으로 이해할 브리핑**을 제공한다.
- 기술 은어(훅, 상태관리, 렌더링 등)를 피하고 "사용자가 무엇을 보는지 / 어떤 동작을 하는지 / 시스템이 내부적으로 어떻게 돕는지" 관점의 비유로 설명한다.
- 각 Phase 완료 시 Phase 전체를 종합하는 **마스터 브리핑**을 제공한다.

### 규칙 5: 정확도·신뢰 최우선 (SnapMath 핵심)

- **검산을 통과한 답만 사용자에게 노출한다.** 풀이(Stage B) → 독립 검산(Stage C) 결과가 일치할 때만 화면에 표시한다.
- 사진 판독 신뢰도가 임계값 미만이면 풀이를 시작하지 말고 **재촬영을 요청**한다.
- 확신이 없으면 **솔직히 인정**한다("이 문제는 자신이 없어, 선생님께 물어보자"). 추측으로 답하지 않는다.
- **학년 초과 개념 금지**: 해당 학년 `curriculum_map`에서 배우는 방법으로만 설명한다(예: 4학년에게 방정식/미지수 x 사용 금지).

### 규칙 6: 아동 데이터 보호 (Privacy by Design)

- **최소 수집**: 학습 신호(단원·정답여부·시도·시간)는 풍부하게, 식별정보(실명·학교·연락처)는 받지 않는다. `user_id`는 가명 식별자.
- **사진 비저장 기본값**: 원본 사진은 텍스트 추출 후 폐기. 저장이 불가피하면 짧은 보관기간 + 비공개 권한.
- **부모 동의**: 만 14세 미만 개인정보 수집/저장 기능은 법정대리인 동의 플로우를 거친다.
- **부모 계정 모델**: 아이가 직접 가입하지 않는다. **부모(성인)가 회원가입**하고 그 아래 **자녀 프로필**(별명·학년만)을 만든다. 아동 개인정보 수집·이용 동의는 자녀 프로필 생성 시점에 부모로부터 받는다. 자녀 자격증명은 수집하지 않는다.
- **문항 풀은 신원과 분리**: 업로드/생성 문항은 `questions` 풀에 문제·풀이·정답만 저장하고 누가 올렸는지는 저장하지 않는다(개인정보 아님). 사진 속 이름 등 비문제 텍스트는 저장 금지. 아이↔문항 연결은 `learning_history.question_id`로만.
- **실데이터 = 유료 모델**: 실제 학생 데이터가 흐르기 시작하면 학습 미사용이 보장되는 유료 Tier/Vertex AI로 전환한다.

---

## 2026 UIUX 디자인 트렌드 반영 지침

> SnapMath는 **초등 4~6학년 아동용 교육 앱**이다. 최신 트렌드 중 학습 경험·신뢰·접근성에 직접 기여하는 것만 선별 적용한다. 트렌드를 위한 트렌드가 아니라 **이해도·재미·리텐션 향상**이 목표다.
> 원칙: *모든 패턴을 체크리스트처럼 넣지 않는다. 문제에 맞는 핵심 몇 개를 정확히 실행한다.*

### 디자인 토큰 (먼저 합의)

```css
/* globals.css @theme (Tailwind v4) — 예시 값, 구현 시 미세 조정 */
@theme {
  --color-brand: #3B82F6;        /* 하늘/파랑: 신뢰 + 친근 (메인) */
  --color-brand-soft: #DBEAFE;   /* 연한 파랑 배경 */
  --color-accent: #FCD34D;       /* 따뜻한 노랑: 즐거움/포인트 */
  --color-success: #34D399;      /* 정답/성공 */
  --color-gentle: #A78BFA;       /* 교정/힌트: 보라 계열(빨강 경고톤 금지) */
  --radius-card: 1rem;           /* rounded-2xl 기본 */
  --font-sans: "Pretendard", system-ui, sans-serif; /* 한글 가독성 */
}
```
- **컬러**: 메인 파랑 + 포인트 노랑. 오답/교정에 **빨간 경고톤 금지**, 부드러운 보라/파랑 사용.
- **타이포**: 아동 대상이라 본문 큼직하게(최소 16px, 풀이/문제 영역 18~24px). 한글 가독성 폰트.
- **모양**: 카드 `rounded-2xl`(16px), 버튼 둥글고 큼. 여백 일관성(`gap-4`/`gap-6`).
- **터치 타겟**: 모바일 최소 48×48px(아동 손가락 고려, 일반 권장치보다 크게).

### T1. 마이크로 인터랙션 (Micro-Interactions)

작은 인터랙션에 의미 있는 시각 피드백을 줘 몰입감을 높인다.
- 풀이 줄이 한 줄씩 부드럽게 페이드인되는 타이핑 애니메이션 [2-5]
- AI 분석 중 "문제 읽는 중…" 점 3개 바운스 인디케이터 [2-2]
- 버튼 호버/탭 시 미세한 scale + tactile(살짝 눌리는) 피드백
- 정답 입력 폼 제출 시 즉각적 상태 전환 [3-3]
- **구현 원칙**: `framer-motion`만 사용. `prefers-reduced-motion` 존중(모션 최소화). 애니메이션 300ms 이하로 학습 흐름 방해 금지.

### T2. 감성적 UI (Emotional UI) — 항상 긍정 강화

아동의 감정에 반응해 학습 지속률을 높인다. **부정·평가 표현 절대 금지.**
- 정답: 밝고 경쾌한 톤 + 컨페티 [3-3]
- 오답: "거의 다 왔어! 여기만 다시 볼까?" 식 부드러운 격려 + 힌트 [3-4]
- 판독 실패: "사진이 잘 안 보여요. 한 번 더 찍어줄래?" (탓하지 않는 톤) [2-3]
- 교정/힌트 카드는 빨강이 아닌 보라/파랑 부드러운 톤.

### T3. 게이미피케이션 (Gamification) — 최소·긍정만

교육 앱 리텐션 검증 전략. 단, 학습을 방해하지 않게 최소 적용.
- 정답 시 즉각 보상(컨페티, 0.5초 이내) [3-3]
- (Phase 4) 학습 기록·정답률 추이·연속 학습일 시각화 [4-4]
- **"벌칙" 요소 절대 없음. 항상 긍정적 강화만.** 리더보드 등 경쟁 요소는 MVP 제외.

### T4. 다크 모드 (Dark Mode) — 기본 기능

야간 숙제 시 눈 피로 감소, OLED 절약. 2026 기준 필수.
- Tailwind v4 다크 모드 전역 지원 + 시스템 설정 자동 감지 + 수동 토글.
- 라이트 모드 우선 개발 후 일괄 적용([4] 마무리 단계).
- 색상 대비 WCAG AA(4.5:1) 이상. 다크 배경은 순수 검정 대신 다크 그레이(#1a1a1a 계열).

### T5. 접근성 우선 (Accessibility-First) — 처음부터

2026 기준 선택이 아닌 기본. 모든 사용자가 차별 없이 쓰도록.
- 키보드 네비게이션(Tab/Enter/Space), 포커스 링 유지.
- 스크린리더: 문제/풀이/결과 영역에 `aria-label`·`aria-live`.
- **색상만으로 정보 전달 금지**: 정답=초록+체크아이콘, 오답=색+아이콘 이중 표시.
- 큰 터치 타겟(48×48px+), 충분한 대비.

### T6. 미니멀 + 콘텐츠 퍼스트

장식보다 콘텐츠(문제·풀이)에 집중.
- 한 화면에 한 가지 핵심 행동. 불필요한 사이드바·광고 없음.
- 카드 기반: 문제 카드 / 풀이 카드 / 도전 카드를 독립 카드로 구분.
- 풀이가 화면 주인공이 되도록 여백과 위계 설계.

### T7. 외과적 글래스모피즘 / 부드러운 깊이 (절제 사용)

2026엔 전체 도배가 아니라 **오버레이에만** 쓴다.
- 힌트 팝업·로딩 오버레이·축하 모달 등 떠 있는 레이어에만 반투명+블러 적용.
- 본문 카드엔 과한 투명도 금지(가독성 우선).

### T8. AI 협업 인터페이스 (AI Collaboration UI) — "또래 짝꿍"

단순 챗봇이 아닌 "함께 푸는 친구"의 존재감.
- AI 짝꿍에 이름/아바타 부여(예: "스냅이").
- 풀이를 기계적 나열이 아니라 "같이 해보자!" 톤으로 한 단계씩.
- 판독 확인·도전 유도도 친구가 말 거는 톤으로.

### T9. 맥락적 프라이버시 표시 (아동 앱 필수)

데이터 사용을 설정 메뉴에 숨기지 말고 **그 자리에서 쉬운 말로** 설명.
- 사진 업로드 시: "사진은 문제를 읽는 데만 쓰고 저장하지 않아요" 같은 안내.
- 저장 기능 켤 때: 부모용 동의·설명 화면(무엇을, 왜, 언제 지우는지).

---

## MVP 단계별 AI 에이전트 프롬프트 세트

> 각 기능 단위 개발 시 아래 프롬프트를 그대로 전달합니다. 기능 번호와 `plan.md` 참조 관계가 매핑되어 있습니다. (T1~T9는 위 디자인 지침 항목)

---

### 🔧 Phase 1: 핵심 엔진 (판독 → 풀이 → 검산)

#### [1-1] 프로젝트 초기 셋업
```
plan.md의 기술 스택과 아키텍처를 참고하여 프로젝트를 초기 셋업해줘.
1. Next.js(App Router) + TypeScript + Tailwind v4 프로젝트를 생성해줘.
2. 폴더 구조: src/app, components, stores, hooks, lib, types.
3. Zustand 스토어 뼈대(analyzeStore: 업로드 이미지/분석결과/로딩상태)를 생성해줘.
4. globals.css @theme에 AGENTS.md 디자인 토큰(브랜드 컬러/라운드/폰트)과 다크 모드(class)를 설정해줘.
5. .env.local 플레이스홀더(GEMINI_API_KEY, GEMINI_MODEL=gemini-2.5-flash)를 넣어줘.
6. ESLint + Prettier 설정.
완료 기준: npm run dev → localhost:3000 에러 없이 렌더링.
주의: 주석 최소화. TdT 자체 검증. 완료 후 비개발자 브리핑.
```

#### [1-2] 이미지 업로드 + Base64 변환
```
1-1 확인됐으니 1-2를 진행해줘.
1. 메인 페이지에 단순 <input type="file" accept="image/*" />을 둬.
2. 선택한 이미지를 Base64로 변환하고 analyzeStore에 저장하는 로직(lib/utils/imageToBase64.ts).
3. 미리보기 썸네일 표시. 파일 형식/용량(예: 10MB 초과) 검증 + 친절한 에러 메시지.
완료 기준: 이미지 선택 → 썸네일 표시 + 콘솔에 Base64 길이 출력.
주의: 주석 최소화. 완료 후 비개발자 브리핑.
```

#### [1-3] Stage A — 판독 API (/api/analyze)
```
1-2 확인됐으니 1-3을 진행해줘. (규칙 5 정확도 최우선)
1. /api/analyze/route.ts 생성. 서버에서만 GEMINI_API_KEY 사용(클라이언트 노출 금지).
2. 멀티모달 프롬프트로 이미지에서 문제 텍스트를 추출하고, 판독 신뢰도(read_confidence 0~1)와 is_math_problem을 반환하게 해줘.
3. plan.md "AI 응답 JSON 스키마"를 강제(JSON 외 출력 금지). 파싱 실패 시 1회 재시도 + 폴백.
4. read_confidence가 임계값(예: 0.6) 미만이면 풀이로 넘기지 않고 재촬영 신호를 반환.
완료 기준: 수학 문제 사진 → extracted_question + read_confidence JSON 확인. 흐릿한 사진 → 낮은 confidence.
주의: 주석 최소화. 완료 후 비개발자 브리핑.
```

#### [1-4] Stage B — 풀이 + 교육과정 매핑
```
1-3 확인됐으니 1-4를 진행해줘.
1. 확인된 문제 텍스트로 단계별 풀이(step_by_step_solution 배열)와 final_answer를 생성.
2. plan.md의 curriculum_map(학년/학기/단원)을 시스템 프롬프트에 주입해 curriculum을 채우고, "해당 학년이 배우는 방법으로만 설명"하도록 강제(규칙 5: 학년 초과 개념 금지).
3. 설명 루브릭: ①무엇을 구하는지 ②어떤 개념을 쓰는지 ③한 단계씩 왜. 한 단계당 한 생각.
완료 기준: 문제 입력 → 학년 수준에 맞는 단계별 풀이 + curriculum 매핑 JSON 확인.
주의: 주석 최소화. 완료 후 비개발자 브리핑.
```

#### [1-5] Stage C — 자체 검산 + 유사 문항 생성
```
1-4 확인됐으니 1-5를 진행해줘. 이게 정확도의 핵심이야(규칙 5).
1. final_answer를 독립적으로 다시 검산(별도 풀이로 재도출)하여 일치 시 verified=true.
2. 불일치 시 verified=false로 두고, 사용자에겐 "자신 없음" 응답을 내보내도록 신호.
3. similar_question(question / 독립 검산된 correct_answer / hint) 생성. 정답은 반드시 검산 후 확정(환각 금지).
완료 기준: 정상 문제 → verified=true + 검증된 유사문항. 의도적으로 모호한 입력 → verified=false 경로 동작.
주의: 주석 최소화. 완료 후 비개발자 브리핑.
```

#### [1-6] Raw JSON 출력 + 파이프라인 검증
```
1-5 확인됐으니 Phase 1 마지막 1-6을 진행해줘.
1. 화면에 분석 결과 전체 JSON을 raw text로 출력(UI 미적용 단계).
2. A→확인→B→C 파이프라인을 엔드투엔드로 호출해보고, 각 단계 분기(저신뢰/검산실패)가 의도대로 동작하는지 확인.
완료 기준: 사진 업로드 → 전체 JSON + verified/confidence 필드 정상. Phase 1 마스터 브리핑 제공.
주의: 주석 최소화.
```

---

### 🎨 Phase 2: 인터랙티브 UI + 수식 렌더링

#### [2-1] 디자인 시스템 셋업
```
Phase 1 확인됐으니 2-1을 진행해줘. AGENTS.md 디자인 토큰/트렌드 지침을 따라.
1. globals.css @theme에 브랜드/포인트/성공/교정 컬러, 라운드, 폰트 토큰 확정.
2. 공통 컴포넌트: Button(대형·둥근, 호버 scale, T1), Card(rounded-2xl), Spinner/LoadingDots(T1).
3. 다크 모드 토큰 세트(T4) + 시스템 감지 + 수동 토글.
4. 접근성 기본(T5): 포커스 링, 48×48 터치 타겟, 대비 점검.
완료 기준: 샘플 페이지에서 버튼/카드/로딩/다크모드 토글 정상.
주의: 인라인 스타일 금지(Tailwind만). 주석 최소화. 비개발자 브리핑.
```

#### [2-2] 홈 화면 + 촬영/업로드 + 로딩
```
2-1 확인됐으니 2-2를 진행해줘.
1. 홈: 큰 "📷 사진 찍기 / 🖼 사진 올리기" 버튼 2개(모바일 우선, T6 콘텐츠 퍼스트).
2. 모바일 카메라 캡처(capture 속성) + 파일 업로드 모두 지원.
3. 분석 중 "스냅이가 문제 읽는 중…" 점3개 바운스 인디케이터(T1, T8 AI 짝꿍 톤).
4. 업로드 직전 맥락적 안내(T9): "사진은 문제를 읽는 데만 써요".
완료 기준: 홈 → 사진 선택 → 로딩 표시 → 1-3 API 호출 연결.
주의: 주석 최소화. 비개발자 브리핑.
```

#### [2-3] 판독 확인 화면 ("이 문제 맞아?")
```
2-2 확인됐으니 2-3을 진행해줘. (정확도 1등 공신 — 규칙 5)
1. extracted_question을 큰 글씨로 보여주고 "이 문제 맞아? [응, 맞아 / 다시 찍을래]" 선택 UI.
2. read_confidence 낮으면 풀이로 넘기지 않고 재촬영 유도(T2 부드러운 톤).
3. "응" → 1-4/1-5 풀이·검산 트리거. "다시" → 홈으로.
완료 기준: 사진 → 추출 문제 확인 화면 → 확인 시 풀이 진행, 재촬영 시 복귀.
주의: 주석 최소화. 비개발자 브리핑.
```

#### [2-4] KaTeX 수식 렌더 컴포넌트
```
2-3 확인됐으니 2-4를 진행해줘.
1. KaTeX 설치 + components/math/MathText.tsx: 텍스트 내 수식 구간을 KaTeX로 렌더.
2. 분수/지수/도형 기호 등 초등 범위 수식 정상 출력 테스트.
3. 다크 모드에서도 수식 가독성 보장(T4).
완료 기준: 분수·소수·각도 등이 포함된 풀이 텍스트가 깨지지 않고 렌더.
주의: 주석 최소화. 비개발자 브리핑.
```

#### [2-5] 단계별 풀이 타이핑 애니메이션
```
2-4 확인됐으니 Phase 2 마지막 2-5를 진행해줘.
1. step_by_step_solution 배열을 1.5초 간격으로 한 줄씩 페이드인(framer-motion, T1).
2. 각 줄은 MathText로 렌더. 마지막 줄에 final_answer 강조.
3. prefers-reduced-motion 시 즉시 전체 표시(T5).
4. verified=false면 풀이 대신 "이 문제는 자신이 없어, 선생님께 물어보자" 안내(규칙 5).
완료 기준: 검증된 풀이 → 한 줄씩 타이핑 노출 + 수식 렌더. Phase 2 마스터 브리핑.
주의: 주석 최소화.
```

---

### 🏆 Phase 3: 유사 문항 도전 + 정답 루프

#### [3-1] 도전 섹션 활성화
```
Phase 2 확인됐으니 3-1을 진행해줘.
1. 풀이 타이핑 종료 이벤트 감지 → 하단 "내가 풀어보기" 섹션을 부드럽게 등장(T1).
2. AI 짝꿍 톤으로 유도(T8): "이번엔 네가 풀어볼 차례야!".
완료 기준: 풀이 종료 후 도전 섹션 자동 활성화.
주의: 주석 최소화. 비개발자 브리핑.
```

#### [3-2] 유사 문제 렌더 + 답 입력 폼
```
3-1 확인됐으니 3-2를 진행해줘.
1. similar_question.question을 MathText로 렌더.
2. 답 입력 폼(숫자/텍스트) + 큰 제출 버튼(T5 터치 타겟).
3. 입력 중 즉각적 미세 피드백(T1).
완료 기준: 유사 문제 표시 + 답 입력/제출 동작.
주의: 주석 최소화. 비개발자 브리핑.
```

#### [3-3] 정답 비교 + 정답 시 컨페티
```
3-2 확인됐으니 3-3을 진행해줘.
1. 입력값과 검산된 correct_answer 비교(공백/단위/형식 정규화 후 비교).
2. 정답 → canvas-confetti 컨페티 + 밝은 격려(T2/T3), 0.5초 이내 즉각 보상.
3. 정답 표시는 색+체크아이콘 이중 표시(T5).
완료 기준: 정답 입력 → 컨페티 + 축하 메시지.
주의: 주석 최소화. 비개발자 브리핑.
```

#### [3-4] 오답 시 힌트 + 재시도 (감성 UI)
```
3-3 확인됐으니 Phase 3 마지막 3-4를 진행해줘. (규칙 2 감성 UI, 부정 표현 금지)
1. 오답 → similar_question.hint 노출 + "거의 다 왔어! 여기만 다시 볼까?" 톤(T2).
2. 정답을 바로 알려주지 않고 재시도 유도. 힌트/교정은 보라·파랑 부드러운 톤(빨강 금지).
3. 여러 번 시도해도 막히면 부드럽게 풀이 다시보기 제안.
완료 기준: 오답 → 힌트 + 재시도, 정답 직접 노출 안 함. Phase 3 마스터 브리핑.
주의: 주석 최소화.
```

---

### 💾 Phase 4: 부모 계정 + 학습 이력 DB

> 아이는 로그인하지 않는다. 부모가 한 번 가입·로그인하고 자녀 프로필을 만들면, 아이는 그 프로필로 학습한다. 부모는 자녀 진척을 본다. (규칙 6)

#### [4-1] Supabase 연동 + 스키마 (RLS)
```
Phase 3 확인됐으니 4-1을 진행해줘. (규칙 6 준수)
1. lib/supabase/client.ts(브라우저), server.ts(서버) 클라이언트.
2. plan.md DDL대로 테이블 생성: child_profiles(부모 소유 자녀 프로필), learning_history(자녀 프로필별 학습 신호). 부모 계정은 Supabase Auth(auth.users) 사용.
3. Row Level Security: 부모는 자기 소유 자녀 프로필과 그 학습 기록만 접근 가능.
4. 식별정보 컬럼은 두지 않음(자녀는 별명·학년만).
완료 기준: 테이블 생성 + RLS로 타 부모 데이터 접근 차단 확인.
주의: 주석 최소화. 비개발자 브리핑.
```

#### [4-2] 부모 회원가입/로그인 + 동의
```
4-1 확인됐으니 4-2를 진행해줘. (규칙 6)
1. Supabase Auth로 부모 회원가입/로그인(이메일 또는 Google OAuth). 가입은 성인(부모) 기준.
2. Header에 로그인/로그아웃 UI(프로필 아바타+드롭다운).
3. 로그인 안 한 상태에서도 Phase 1~3 학습은 1회성(미저장)으로 가능. 저장은 로그인+동의 후에만.
완료 기준: 부모 가입 → 로그인 → 로그아웃 → 재로그인 동작.
주의: 주석 최소화. 비개발자 브리핑.
```

#### [4-3] 자녀 프로필 생성/선택 + 동의 게이트
```
4-2 확인됐으니 4-3을 진행해줘. (규칙 6, T9 맥락적 프라이버시)
1. 자녀 프로필 생성 폼: 별명 + 학년 + 학기 (실명·학교·연락처 받지 않음).
2. 프로필 생성 시 "무엇을/왜/언제까지 저장하는지" 쉬운 말 안내 + 법정대리인(부모) 동의 체크. 동의 전에는 학습 기록 저장 안 함.
3. 자녀가 여러 명이면 프로필 스위처(아이가 자기 프로필 선택).
완료 기준: 프로필 생성(동의 포함) → 활성 프로필 선택 → 이후 학습이 해당 프로필에 연결.
주의: 주석 최소화. 비개발자 브리핑.
```

#### [4-4] 학습 결과 저장 (최소 수집)
```
4-3 확인됐으니 4-4를 진행해줘.
1. 풀이/도전 완료 시 learning_history INSERT: child_profile_id/grade/semester/unit/concept/question_text/is_correct/attempts/time_spent_s.
2. 원본 사진은 저장하지 않음(기본값). question_text만 저장.
완료 기준: 도전 완료 → 활성 자녀 프로필에 학습 신호 1건 저장, 사진 미저장 확인.
주의: 주석 최소화. 비개발자 브리핑.
```

#### [4-5] 부모 대시보드 /history (오답 노트)
```
4-4 확인됐으니 Phase 4 마지막 4-5를 진행해줘.
1. /history: 자녀별로 틀린 단원·날짜·정답률 추이를 카드/리스트로(T6). 자녀 여러 명이면 프로필별 보기.
2. 오답 노트: 단원별로 다시 도전 진입.
3. 다크 모드 최종 점검(T4) + 접근성 최종 점검(T5).
완료 기준: 부모가 대시보드에서 자녀 학습 기록·오답·추이를 본다. Phase 4 + 전체 마스터 브리핑(사용자 여정 관점).
주의: 주석 최소화.
```

---

### 🗂 Phase 5: 문항 풀(DB) + 별도 서비스 확장 기반

> 업로드/생성 문항을 **신원과 분리된 풀**에 적립해 재사용·큐레이션하고, 나중에 독립 서비스로 분리 가능하게 캡슐화한다. (규칙 6: 문항 풀은 신원과 분리)

#### [5-1] 문항 풀 스키마 + 정규화/중복 제거
```
Phase 4 확인됐으니 5-1을 진행해줘.
1. plan.md DDL대로 questions 테이블 생성(source/question_text/normalized_hash/grade/semester/unit/concept/difficulty/solution_steps/final_answer/hint/verified/review_status/usage_count).
2. lib/questionPool/normalize.ts: 공백·기호 정규화 후 해시 생성 함수. normalized_hash UNIQUE로 중복 방지.
3. questions에는 누가 올렸는지(아이/부모 식별자) 저장하지 않음. 문제 텍스트에 이름 등 비문제 내용 들어가지 않게 가드.
완료 기준: 동일 문제 2회 적재 시 1건만 유지 + usage_count 증가.
주의: 주석 최소화. 비개발자 브리핑.
```

#### [5-2] 파이프라인 → 풀 적재
```
5-1 확인됐으니 5-2를 진행해줘.
1. Stage C에서 verified=true가 된 업로드 문항을 questions에 upsert(source='uploaded').
2. 생성된 유사 문항도 정답 검산 후 upsert(source='generated').
3. 적재는 검산 통과분만. 중복이면 usage_count만 증가.
완료 기준: 문제 풀이/유사문항 생성 → questions에 source 태깅되어 적립.
주의: 주석 최소화. 비개발자 브리핑.
```

#### [5-3] 풀 우선 조회(재사용)
```
5-2 확인됐으니 5-3을 진행해줘. (비용↓·일관성↑)
1. 유사 문항이 필요할 때, 먼저 같은 unit·concept·(가능하면 difficulty)의 verified·approved 문항을 풀에서 조회.
2. 적합 문항이 있으면 모델 호출 없이 재사용(최근 푼 것과 중복은 피함).
3. 미스일 때만 생성 → 5-2 경로로 적재.
완료 기준: 풀에 재고가 있으면 모델 호출 없이 유사문항 제공, 재사용률 측정 가능.
주의: 주석 최소화. 비개발자 브리핑.
```

#### [5-4] 큐레이션/관리 뷰
```
5-3 확인됐으니 5-4를 진행해줘.
1. review_status('auto'|'approved'|'flagged') 운영: 신고/오류 문항을 flagged로 격리(재사용 제외).
2. 관리자용 간단 뷰: 단원별 문항 목록, 승인/격리 토글, 검색.
3. flagged·verified=false 문항은 학생에게 노출/재사용되지 않도록 쿼리에서 제외.
완료 기준: 문항 격리 시 재사용 후보에서 빠짐, 승인 문항만 노출.
주의: 주석 최소화. 비개발자 브리핑.
```

#### [5-5] 풀 모듈 분리 (확장 토대)
```
5-4 확인됐으니 Phase 5 마지막 5-5를 진행해줘.
1. 풀 접근 로직을 lib/questionPool 모듈(또는 /api/questions 내부 API)로 캡슐화 — 메인 앱은 이 인터페이스로만 풀에 접근.
2. 향후 문항 은행·워크시트 등 별도 서비스로 분리할 수 있도록 questions 도메인을 다른 도메인(학습 이력/부모 계정)과 결합도 낮게 유지.
3. README/주석 없이도 모듈 경계가 드러나는 폴더 구조.
완료 기준: 메인 앱이 풀 모듈 인터페이스만으로 동작. Phase 5 마스터 브리핑(이 풀이 어떻게 별도 서비스가 될 수 있는지 포함).
주의: 주석 최소화.
```

---

## 공통 코딩 스타일 가이드

### 네이밍 컨벤션
- 컴포넌트: PascalCase (`MathText.tsx`) · 함수/변수: camelCase (`compareAnswer`)
- 상수: UPPER_SNAKE_CASE (`READ_CONFIDENCE_THRESHOLD`) · 타입/인터페이스: PascalCase (`AnalyzeResult`)
- 파일명: 컴포넌트 PascalCase, 유틸/훅 camelCase

### 코드 구조 원칙
- 한 파일 300줄 이하(초과 시 분리), 한 함수 50줄 이하, 한 컴포넌트 하나의 책임.
- `any` 타입 금지(명시적 타입 정의). 인라인 스타일 금지(Tailwind만).
- AI 응답은 타입으로 정의(`types/analyze.ts`)하고 런타임 파싱 검증.

### 에러 처리 원칙
- 모든 API 호출에 try/catch. 사용자에게 보이는 에러는 친절하고 구체적으로(아동 눈높이).
- 콘솔 에러는 개발 환경에서만(`process.env.NODE_ENV === 'development'`).
- AI/판독/검산 실패 시 반드시 폴백(재촬영 유도 또는 "자신 없음" 안내) — 규칙 5.

### 성능 원칙
- 이미지는 Next.js `Image` 사용. 무거운 비교/정규화는 `useMemo`.
- API 호출 중복 방지(로딩 중 재요청 차단), 무료 티어 429 대비 지수 백오프 + "잠시 후 다시" 안내.
